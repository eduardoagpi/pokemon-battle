version: "3.9"

services:
  key-gen:
    build:
      context: .
      dockerfile: Dockerfile.mongo.init
    volumes:
      - mongo_key:/mnt/shared

  mongo:
    image: mongo:6
    container_name: mongo_db
    hostname: mongo-server
    depends_on:
      key-gen:
        condition: service_completed_successfully
    environment:
      - MONGO_INITDB_ROOT_USERNAME=${MONGO_ROOT_USER}
      - MONGO_INITDB_ROOT_PASSWORD=${MONGO_ROOT_PASSWORD}
    ports:
      - "${MONGO_PORT:-27017}:27017"
    volumes:
      - mongo_data:/data/db
      - mongo_key:/data/configdb
    command: [ "mongod", "--replSet", "rs0", "--bind_ip_all", "--keyFile", "/data/configdb/keyfile" ]
    healthcheck:
      test: [ "CMD", "mongosh", "--eval", "db.adminCommand('ping')" ]
      interval: 5s
      timeout: 5s
      retries: 10

  mongo-init:
    image: mongo:6
    depends_on:
      mongo:
        condition: service_healthy
    entrypoint: >
      mongosh --host mongo-server -u ${MONGO_ROOT_USER} -p ${MONGO_ROOT_PASSWORD} --authenticationDatabase admin --eval "try { rs.status() } catch (err) { rs.initiate({_id:'rs0',members:[{_id:0,host:'mongo-server:27017'}]}) }"

  mongo-express:
    image: mongo-express:latest
    container_name: mongo-express
    restart: always
    ports:
      - "${MONGO_EXPRESS_PORT}:${MONGO_EXPRESS_PORT}"
    environment:
      - ME_CONFIG_MONGODB_URL=mongodb://${MONGO_ROOT_USER}:${MONGO_ROOT_PASSWORD}@mongo-server:27017/?authSource=admin&directConnection=true
      - ME_CONFIG_BASICAUTH_USERNAME=${MONGO_EXPRESS_DASHBOARD_USER}
      - ME_CONFIG_BASICAUTH_PASSWORD=${MONGO_EXPRESS_DASHBOARD_PASSWORD}
    depends_on:
      mongo:
        condition: service_healthy

  backend:
    build:
      context: .
      dockerfile: packages/backend/Dockerfile
      args:
        - PORT=${POKEMON_API_1_PORT}
    container_name: poke-albo-backend
    restart: always
    ports:
      - "${POKEMON_API_1_PORT}:${POKEMON_API_1_PORT}"

  battle-service:
    build:
      context: .
      dockerfile: packages/battle-service/Dockerfile
      args:
        - PORT=${BATTLE_SERVICE_PORT:-4000}
    container_name: poke-albo-battle-service
    restart: always
    ports:
      - "${BATTLE_SERVICE_PORT:-4000}:${BATTLE_SERVICE_PORT:-4000}"
    environment:
      - MONGO_URL=mongodb://${MONGO_ROOT_USER}:${MONGO_ROOT_PASSWORD}@mongo-server:27017/battle-db?authSource=admin
    env_file:
      - .env.prod
    depends_on:
      mongo:
        condition: service_healthy

  frontend:
    build:
      context: .
      dockerfile: packages/frontend/Dockerfile
      args:
        - PORT=80
    container_name: poke-albo-frontend
    restart: always
    ports:
      - "${VITE_PORT:-5000}:80"
    environment:
      - PORT=80
    env_file:
      - .env.prod
    depends_on:
      - backend
      - battle-service

volumes:
  mongo_data:
  mongo_key:
